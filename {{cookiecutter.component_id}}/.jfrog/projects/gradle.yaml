version: 1
type: gradle
resolver:
  repo: mvn
  serverId: artifactory
deployer:
  repo: {{cookiecutter.component_id}}-mvn-dev-local
  serverId: artifactory
  artifactPattern: '[organization]/[module]/[revision]/[artifact]-[revision](-[classifier]).[ext]'
useWrapper: true
